# Mail MCP Server 环境配置文件
# 复制此文件为 .env 并填入您的实际邮件服务器设置

# ======================
# IMAP 服务器配置
# ======================
IMAP_HOST=imap.example.com          # IMAP 服务器地址
IMAP_PORT=993                        # IMAP 端口号
IMAP_USE_SSL=true                   # 是否使用 SSL 连接
IMAP_USERNAME=your_email@example.com # IMAP 用户名（邮箱地址）
IMAP_PASSWORD=your_password          # IMAP 密码或应用专用密码

# ======================
# SMTP 服务器配置
# ======================
SMTP_HOST=smtp.example.com          # SMTP 服务器地址
SMTP_PORT=587                        # SMTP 端口号
SMTP_USE_SSL=false                  # 是否使用 SSL 连接
SMTP_USE_TLS=true                   # 是否使用 TLS 加密
SMTP_USERNAME=your_email@example.com # SMTP 用户名（邮箱地址）
SMTP_PASSWORD=your_password          # SMTP 密码或应用专用密码

# ======================
# 服务器配置
# ======================
SERVER_HOST=localhost               # 服务器监听地址
SERVER_PORT=8000                     # 服务器监听端口
LOG_LEVEL=INFO                       # 日志级别 (DEBUG, INFO, WARNING, ERROR)

# ======================
# 性能配置
# ======================
IMAP_TIMEOUT=30                      # IMAP 连接超时时间（秒）
SMTP_TIMEOUT=30                      # SMTP 连接超时时间（秒）
MAX_ATTACHMENT_SIZE=26214400        # 附件大小限制（字节，默认 25MB）
MAX_RETRIES=3                       # 最大重试次数

# ======================
# API 密钥配置（可选，用于 Task Master AI 功能）
# ======================
# ANTHROPIC_API_KEY="your_anthropic_api_key_here"       # 格式: sk-ant-api03-...
# PERPLEXITY_API_KEY="your_perplexity_api_key_here"     # 格式: pplx-...
# OPENAI_API_KEY="your_openai_api_key_here"             # 格式: sk-proj-...

# ======================
# 常见邮箱服务商配置示例
# ======================

# Gmail 配置:
# IMAP_HOST=imap.gmail.com
# IMAP_PORT=993
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USE_SSL=false
# SMTP_USE_TLS=true
# 注意: Gmail 需要使用应用专用密码

# QQ 邮箱配置:
# IMAP_HOST=imap.qq.com
# IMAP_PORT=993
# SMTP_HOST=smtp.qq.com
# SMTP_PORT=587
# 注意: QQ 邮箱需要使用授权码

# 163 邮箱配置:
# IMAP_HOST=imap.163.com
# IMAP_PORT=993
# SMTP_HOST=smtp.163.com
# SMTP_PORT=994
# 注意: 163 邮箱需要开启 IMAP/SMTP 服务

# 企业邮箱配置示例（阿里云）:
# IMAP_HOST=imap.qiye.aliyun.com
# IMAP_PORT=993
# SMTP_HOST=smtp.qiye.aliyun.com
# SMTP_PORT=465